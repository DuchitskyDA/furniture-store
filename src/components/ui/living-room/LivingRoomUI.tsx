import { ProductPreview } from '../../index';
import styles from './index.module.scss';
export const LivingRoomUI = () => {
  return (
    <section className={styles.preview}>
      <ProductPreview />
    </section>
  );
};
